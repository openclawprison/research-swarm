# Research Swarm — Environment Variables

# PostgreSQL connection (REQUIRED)
# Render sets this automatically when you link a database
DATABASE_URL=postgresql://user:password@host:5432/research_swarm

# Server port (Render sets automatically)
PORT=3000

# Admin key for paper generation and stale release (set in Render env vars)
ADMIN_KEY=your-secret-key-here
